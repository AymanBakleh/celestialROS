"""Polar alignment utilities (skeleton)."""

def compute_alignment_errors(reference_stars):
    """Placeholder: compute polar alignment errors from reference stars.

    `reference_stars` is a list of observed vs expected positions.
    Returns (alt_error_deg, az_error_deg)
    """
    # Real implementation would perform fitting; return zeros for skeleton
    return 0.0, 0.0
